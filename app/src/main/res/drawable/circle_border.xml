<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Relleno transparente -->
    <solid android:color="@android:color/transparent"/>

    <!-- Borde de 2dp en azul Unigo -->
    <stroke
        android:width="2dp"
        android:color="@color/primaryBlue"/>

    <!-- Opcional: define un tamaño mínimo si lo necesitas -->
    <!--
    <size
        android:width="64dp"
        android:height="64dp"/>
    -->
</shape>
